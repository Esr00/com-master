import { TestBed } from '@angular/core/testing';
import { Httpn } from '@angular/common/http';

import { headres} from './headres.eptor'

describe('headres, () => {
  const  HttpeptorFeq, next) =>
    TestBed.runInInjectionContext(() => headresreq, next));

  beforeEach(() => {
    TestBed.configureTestingModule({});
  });

  it('should be created', () => {
    expect(.toBeTruthy();
  });
});
